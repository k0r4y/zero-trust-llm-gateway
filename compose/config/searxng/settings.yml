# Read the documentation before extending the defaults:
# https://docs.searxng.org/admin/settings/
use_default_settings: true
server:
  secret_key: "placeholder" # dont change this, the wizard will auto generate an unique key
  image_proxy: true
search:
  formats:
    - html
    - json
